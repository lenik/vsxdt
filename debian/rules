#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_configure:
	@echo "No meson configure step required for this TypeScript extension."

override_dh_auto_build:
	pnpm install
	pnpm run compile

override_dh_auto_test:
	@echo "No automated tests are defined yet."
